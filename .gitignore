# Secrets
.env
.env.*
!.env.example
*.key

# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/
.pytest_cache/
.ruff_cache/

# Virtual env
venv/
.venv/
env/

# Data: semua di Supabase sekarang, jadi data/ TIDAK ikut git
# Data lokal cuma cache. Sumber kebenaran = Supabase (tabel app_datasets).
data/
mlruns/
logs/
*.parquet

# models/ SENGAJA tidak di-ignore: app butuh models/xgb_demand.pkl saat deploy
# (file kecil ~2MB, bukan hasil scraping, gak ada isu ToS).

# Cache (notebook / scraping)
**/cache/
.cache/

# OS / editor
.DS_Store
.idea/
.vscode/
